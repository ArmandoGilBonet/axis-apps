# Master app for all customer websites
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: clients-apps
  namespace: argocd
spec:
  project: clients
  source:
    repoURL: 'git@github.com:ArmandoGilBonet/axis-apps.git'
    targetRevision: HEAD
    path: clients
    directory:
      recurse: true # Recursively include all subdirectories  
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true