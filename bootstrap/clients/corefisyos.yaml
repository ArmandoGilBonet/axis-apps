apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: stack-corefisyos
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io # Para que borre todo al eliminar este archivo
spec:
  project: clients
  source:
    repoURL: 'git@github.com:ArmandoGilBonet/axis-apps.git'
    targetRevision: HEAD
    path: projects/clients/corefisyos/
    directory:
      recurse: true # <--- CLAVE: Buscará en common, odoo, etc.
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: client-corefisyos # Namespace por defecto (aunque common lo defina)
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true